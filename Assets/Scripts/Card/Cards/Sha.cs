using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Sha : Card
{
    public override string cardName { get ; protected set ; }="Sha";
    public override string description { get ; protected set ; } = "Sha is a card that represents the concept of 'Sha' in the game. It is a powerful card that can be used to inflict damage on opponents or to defend against attacks. The card's abilities and effects may vary depending on the game mechanics and rules.";
    public override Sprite cardImage { get ; protected set ; }
    public override string CardType { get ; protected set ; } = "Sha";

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
